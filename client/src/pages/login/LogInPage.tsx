import { ContentContainer } from "../../components";
import { TelegramWidget } from "../../widgets";

const LogInPage = () => {
  return (
    <ContentContainer center>
      <TelegramWidget />
    </ContentContainer>
  );
};

export { LogInPage };
